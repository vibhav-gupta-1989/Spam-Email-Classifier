# spam-email-classifier

- Built an end-to-end spam email classification pipeline using the SpamAssassin email dataset.
- Implemented email parsing, text preprocessing, URL/number normalization, punctuation removal, lowercasing, and Porter stemming, followed by frequency-based feature vectorization.
- Trained a Random Forest classifier and achieved 95.8% mean accuracy using 5-fold cross-validation.
